package de.muenchen.kvr.buergerverwaltung.buerger.service.gen.rest.search;


import java.lang.reflect.Field;
import java.util.List;
import java.util.ArrayList;
import java.util.stream.Collectors;
import java.util.stream.Stream;
import java.util.stream.StreamSupport;

import org.hibernate.search.exception.EmptyQueryException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.repository.query.Param;
import org.springframework.data.rest.webmvc.BasePathAwareController;
import org.springframework.data.rest.webmvc.PersistentEntityResource;
import org.springframework.data.rest.webmvc.PersistentEntityResourceAssembler;
import org.springframework.hateoas.ExposesResourceFor;
import org.springframework.hateoas.Resources;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;

import de.muenchen.service.QueryService;
import de.muenchen.kvr.buergerverwaltung.buerger.service.gen.domain.Buerger_;
import de.muenchen.kvr.buergerverwaltung.buerger.service.gen.rest.Buerger_Repository;

/*
 * This file will be overwritten on every change of the model!
 * This file was automatically generated by GAIA.
 */
@BasePathAwareController
@ExposesResourceFor(Buerger_.class)
@RequestMapping("/buergers/search")
public class Buerger_SearchController {
		
    @Autowired
    QueryService service;

    @Autowired
    Buerger_Repository repository;

    @RequestMapping(method = RequestMethod.GET, value = "findFullTextFuzzy")
    @ResponseBody
    public ResponseEntity<?> findFullTextFuzzy(PersistentEntityResourceAssembler assembler, @Param("q") String q) {
        if (q == null)
            q = "";

		// Get all fields-names (including superclass fields) annotated with @Field
		List<String> annotatedFields = new ArrayList<String>();
	    Class tmpClass = Buerger_.class;
	    while (tmpClass != null) {
		    annotatedFields.addAll(Stream.of(tmpClass.getDeclaredFields())
                .filter(field -> field.isAnnotationPresent(org.hibernate.search.annotations.Field.class))
                .map(Field::getName)
                .collect(Collectors.toList()));
        	tmpClass = tmpClass.getSuperclass();
	    }

        Stream<Buerger_> buergerStream;
        try {
            buergerStream = service.query(q, Buerger_.class, annotatedFields.toArray(new String[annotatedFields.size()])).stream();
        } catch (EmptyQueryException e) {
            buergerStream = StreamSupport.stream(repository.findAll().spliterator(), false);
        }

        final List<PersistentEntityResource> collect = buergerStream.map(assembler::toResource).collect(Collectors.toList());
        return new ResponseEntity<Object>(new Resources<>(collect), HttpStatus.OK);
	}
}


